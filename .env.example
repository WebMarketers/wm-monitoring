PORT=3000
NODE_ENV=development

# On droplet: /usr/bin/chromium-browser
# On Mac: leave empty (backstop uses its own bundled Chromium)
CHROMIUM_PATH=

# Base URL for the monitoring dashboard (used in Slack notifications)
DASHBOARD_URL=http://localhost:3000
